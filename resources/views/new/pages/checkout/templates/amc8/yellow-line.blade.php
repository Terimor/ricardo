@if (!empty($product->splash_description))
  <div class="yellow-line">
    <div class="container">
      <div class="splash-description">{{ $product->splash_description }}</div>
    </div>
  </div>
@endif
