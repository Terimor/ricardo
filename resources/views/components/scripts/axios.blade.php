@if (!empty($is_upsells_page) && empty($is_smartbell))

  <script
    src="https://cdnjs.cloudflare.com/ajax/libs/axios/0.19.2/axios.min.js"
    onload="js_deps.ready('axios')"
    async></script>

@endif
